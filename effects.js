function toSetup() {
    $('html, body').animate({
        scrollTop: $('#setup').offset().top
    }, 'slow');
};

function toUpload() {
    $('html, body').animate({
        scrollTop: $('#upload').offset().top
    }, 'slow');
};

function toVerify() {
  console.log("verify");
    $('html, body').animate({
        scrollTop: $('#verify').offset().top
    }, 'slow');
};